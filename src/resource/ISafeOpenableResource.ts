/* eslint
    "@typescript-eslint/no-empty-interface": off
*/
import * as api from "pareto-api"

export interface ISafeOpenableResource<OpenedResource> extends api.ISafeOpenableResource<OpenedResource> {
}
